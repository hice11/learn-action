name: context

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Print Password
        # 환경변수 사용
        env:
          MY_PW: ${{secrets.PW}}
        run: echo MY password is $MY_PW
